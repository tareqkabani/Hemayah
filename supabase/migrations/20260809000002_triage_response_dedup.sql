-- ============================================================
--  حزمة 9 أغسطس — المحور ٣ (بوابة الفرز):
--  حذف الصيغة المكرّرة «بناءً على التواصل تم حفظ طلبكم.» من قائمة
--  نصوص إشعار الحفظ (triage_response) — تبقى «تم الحفظ بناءً على طلبكم.»
--  (closeReq_2) هي الصيغة الوحيدة لسبب closeReq.
--
--  البند المهجور يُعطَّل (active=false) ولا يُحذف — سلامة السجلات القديمة:
--  الرسائل المُرسَلة سابقاً بهذا النص تبقى كما أُرسلت، ولا جدول أعمال
--  يخزّن item_key لهذه القائمة فلا ترحيل صفوف مطلوباً.
--  idempotent: إعادة التطبيق بلا ضرر.
-- ============================================================

update reference_items
   set active = false
 where list_key = 'triage_response'
   and item_key = 'closeReq_1';
